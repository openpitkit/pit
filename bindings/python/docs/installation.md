# Installation

Install the published Python package from PyPI:

```bash
pip install openpit
```

For local development from this repository, install the Python bindings package:

```bash
pip install ./bindings/python
```
